<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.vue.model.mapper.TodoMapper">

	<insert id="writeTodo" parameterType="todoDto">
		insert into board (userid, content, status)
		values (#{userid}, #{content}, #{status})
	</insert>
	
	<select id="listTodo" resultType="todoDto">
		select todono, userid, content, status
		from todo
		order by todono desc
	</select>

	<delete id="deleteTodo" parameterType="int">
		delete from todo
		where todono = #{todono}
	</delete>
	
	<update id="modifyTodo" parameterType="todoDto">
		update board
		set userid = #{userid}, content = #{content}, status = #{status}
		where todono = #{todono}
	</update>

</mapper>

